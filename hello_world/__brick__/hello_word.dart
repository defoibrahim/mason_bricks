import 'package:flutter/material.dart';

class {{name}} extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello, {{name}}!, this is your first brick!'),
    );
  }
}
